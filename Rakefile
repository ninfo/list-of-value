# -*- ruby -*-

require 'rubygems'
require 'rake'
#require 'hoe/rake'

namespace :listofvalue do
  task :install do
    run "rails generate model CreateListOfValue lov_type:string value:string locale:string display_value:string description:string"
  end
end
